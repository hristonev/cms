{% extends 'base.html.twig' %}

{% block body %}
    {#{% include 'blocks/header.html.twig' %}#}
    <div class="top-main-buffer"></div>
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>{% trans from "security" %}user_change_password.heading{% endtrans %}</h1>
            </div>
        </div>
        {{ form_start(form) }}

        {{ form_errors(form) }}

        {{ form_row(form.oldPassword) }}

        {{ form_row(form.newPassword) }}

        <button type="submit" class="btn btn-success">
            {% trans from "form" %}change.btn{% endtrans %}
            <span class="fa fa-lock"></span>
        </button>

        {{ form_end(form) }}
    </div>
{% endblock %}

